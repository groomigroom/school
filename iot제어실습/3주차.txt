http://syncsignal.co.kr/EduGrading/VisitStudSv.do


ddr 레지스터는 입출력을 설정하는거
1주면 5볼트가 나가고 0주면 0볼트가 나간다.

PA0는 최하위 비트임.

다이오드 오른족이 마이너스인다.

접지는 항상 0볼트

set as startup project


----------------------------------------------------


#define F_CPU 16000000

#include <avr/io.h>
#include <stdio.h>
#include <util/delay.h>

uint8_t autoGauge(uint8_t led) {
	return led << 1;
}

int main(void)
{
	DDRA = 0xFF;
	//입력으로 사용할건지 출력으로 사용할 건지 하는거
	uint8_t led = 1;
    /* Replace with your application code */
    while (1) 
    {
		PORTA = led;
		led = autoShift(led);
		_delay_ms(1000);
    }
}



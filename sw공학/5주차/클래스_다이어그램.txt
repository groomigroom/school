기능 하나하나를 묶어서 하는 프로젝트 내에서 클래스이다.

클래스들의 기능 등등을 내포하고 있는지 표현한게 클래스 다이어그램이다.

메서드는 함수
()

클래스 이름 -> 클래스는 다른 클래스와 구별되는 이름을 가짐.
속성 -> 속성 이름은 소문자로 두 단어는 groomiGroom 이런식으로
메서드 -> 클래스가 외부의 다른 객체에게 제공할 서비스와 기능.

이런식으로 표현함

